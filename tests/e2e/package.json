{
  "name": "areg-e2e",
  "version": "1.0.0",
  "description": "AREG Playwright smoke tests",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0"
  }
}
